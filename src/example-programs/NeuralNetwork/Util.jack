class Util {
    static int next;

    function void config(int seed) {
        let next = seed;
    }

    function int random() {
        let next = (next * 17565) + 2489;
        return next;
    }
}