/**
 * A wrapper around a simple LCG
 */
class Util {
    static int next;

    function void init() {
        // The answer to the ultimate question of life, the universe, and everything
        let next = 42;
    }

    function int random() {
        let next = (next * 25173) + 13849;
        return next;
    }
}
